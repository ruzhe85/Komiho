<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Foreground-service permissions kept because WorkManager's
         SystemForegroundService (dataSync type) is merged into the manifest
         and requires them on Android 14+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Komiho: 真实路径模式（Android 11+）需所有文件访问权限，才能绕过 SAF 直接 File 访问漫画目录。 -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- 存储运行时权限组：部分 ROM（ColorOS 等）的「权限管理」只有对声明了存储权限的 app
         才显示「存储 → 允许管理所有文件」入口；只声明 MANAGE 的话入口不出现（对齐
         ComicScreen：仅声明 WRITE_EXTERNAL_STORAGE 即可见）。授权判定仍走 MANAGE。 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- The following were inherited from the MihonSY fork and are not used
         by a LAN-only Komga client (storage / extension management / boot /
         query packages / notifications / locale). -->

    <!-- Remove unnecessary permissions from Firebase dependency -->
    <uses-permission
        android:name="com.google.android.finsky.permission.BIND_GET_INSTALL_REFERRER_SERVICE"
        tools:node="remove" />

    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />

    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION"
        tools:node="remove" />

    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove" />

    <!-- Komiho: 移除第三方 AAR 合并进来的死权限——
         CAMERA 来自 zxing-android-embedded（仅用其 Intents 常量类，无扫码功能）；
         SYSTEM_ALERT_WINDOW 来自 Shizuku api（无悬浮窗代码）；
         QUERY_ALL_PACKAGES 为扩展扫描遗留（扩展组已隐藏，纯 Komga 客户端不保留）。 -->
    <uses-permission
        android:name="android.permission.CAMERA"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.SYSTEM_ALERT_WINDOW"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:node="remove" />

    <!-- Komiho: 包可见性白名单（替代 QUERY_ALL_PACKAGES）——
         WebViewUtil.spoofedPackageName 需要看见 chrome/系统设置/YT-TV。
         注意：不能加 VIEW+https intent（浏览器可见性）——Google 政策与鸿蒙
         均视其为「读取已安装应用列表」，会重新触发风险标签。 -->
    <queries>
        <package android:name="com.google.android.webview" />
        <package android:name="com.android.chrome" />
        <package android:name="com.android.settings" />
        <package android:name="com.google.android.youtube.tv" />
    </queries>

    <application
        android:name=".App"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:preserveLegacyExternalStorage="true"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tachiyomi">

        <!-- Komga client: connection setup (M1). Currently launched manually
             from the launcher until the main UI is replaced. -->
        <activity
            android:name="app.mihonsy.komga.ui.KomgaConnectActivity"
            android:exported="true"
            android:theme="@style/Theme.Tachiyomi.SplashScreen">
        </activity>

        <!-- Komiho M3: main tabbed activity (Home/Library/Lists/Settings).
             Replaces the removed single-screen KomgaHomeActivity.
             configChanges keeps tab/scroll state across orientation changes. -->
        <activity
            android:name="app.mihonsy.komga.ui.KomgaMainActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
        </activity>

        <!-- Komiho M3: readlist detail (books inside a reading list) -->
        <activity
            android:name="app.mihonsy.komga.ui.KomgaReadlistActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
        </activity>

        <!-- Komiho M3: collection detail (series inside a Komga collection) -->
        <activity
            android:name="app.mihonsy.komga.ui.KomgaCollectionActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
        </activity>

        <!-- Komiho M3: full list for a Home section ("全部" button) -->
        <activity
            android:name="app.mihonsy.komga.ui.KomgaSectionListActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
        </activity>

        <activity
            android:name="app.mihonsy.komga.ui.KomgaSeriesActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
        </activity>

        <!-- Komga client: app entry point (M3-1). Routes to the main tabbed
             activity when a connection is configured, otherwise to the
             onboarding welcome screen (SY: 多源化——不再强制 Komga 设置). -->
        <activity
            android:name="app.mihonsy.komga.ui.KomgaLauncherActivity"
            android:exported="true"
            android:theme="@style/Theme.Tachiyomi.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SY --> Komiho Onboarding: 首启欢迎页（三来源任选，可跳过） -->
        <activity
            android:name="app.mihonsy.komga.ui.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.Tachiyomi.SplashScreen" />

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Tachiyomi.SplashScreen">
            <!-- Legacy deeplink to add extension store -->
            <intent-filter android:label="@string/action_addExtensionStore">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="tachiyomi" />
                <data android:host="add-repo" />
            </intent-filter>

            <!-- Deeplink to add extension store -->
            <intent-filter android:label="@string/action_addExtensionStore">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="mihon" />
                <data android:host="extension-store" />
            </intent-filter>

            <!-- Open backup files -->
            <intent-filter android:label="@string/pref_restore_backup">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:host="*" />
                <data android:mimeType="*/*" />
                <!--
                Work around Android's ugly primitive PatternMatcher
                implementation that can't cope with finding a . early in
                the path unless it's explicitly matched.

                See https://stackoverflow.com/a/31028507
                -->
                <data android:pathPattern=".*\\.tachibk" />
                <data android:pathPattern=".*\\..*\\.tachibk" />
                <data android:pathPattern=".*\\..*\\..*\\.tachibk" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.tachibk" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\.tachibk" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\..*\\.tachibk" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\..*\\..*\\.tachibk" />
            </intent-filter>

            <!--suppress AndroidDomInspection -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".crash.CrashActivity"
            android:exported="false"
            android:process=":error_handler" />

        <activity
            android:name=".ui.deeplink.DeepLinkActivity"
            android:exported="true"
            android:label="@string/action_search"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
                <action android:name="com.google.android.gms.actions.SEARCH_ACTION" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="eu.kanade.tachiyomi.SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter android:label="@string/app_settings">
                <action android:name="android.intent.action.APPLICATION_PREFERENCES" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <activity
            android:name=".ui.reader.ReaderActivity"
            android:exported="false"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="com.samsung.android.support.REMOTE_ACTION" />
            </intent-filter>

            <meta-data
                android:name="com.samsung.android.support.REMOTE_ACTION"
                android:resource="@xml/s_pen_actions" />
        </activity>

        <activity
            android:name=".ui.security.UnlockActivity"
            android:exported="false"
            android:theme="@style/Theme.Tachiyomi" />

        <activity
            android:name=".ui.webview.WebViewActivity"
            android:configChanges="uiMode|orientation|screenSize"
            android:exported="false" />




        <receiver
            android:name=".data.notification.NotificationReceiver"
            android:exported="false" />


        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>


        <meta-data
            android:name="android.webkit.WebView.EnableSafeBrowsing"
            android:value="false" />
        <meta-data
            android:name="android.webkit.WebView.MetricsOptOut"
            android:value="true" />

        <!-- Disable for manual opt-in -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <!-- Disable advertising ID collection for Firebase -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />


    </application>

    <uses-sdk tools:overrideLibrary="rikka.shizuku.api"
        tools:ignore="ManifestOrder" />

</manifest>
